# Blockexplorer

Pokretanje backenda aplikacije:

    cd backend
	pip install -r requirements.txt
    manage.py runserver

Pokretanje frontend aplikacije:

    cd frontend
    npm install
    npm start

